#!/usr/bin/env ruby

require 'pipely'
require 'readline'
require File.dirname(__FILE__) + '/../src/shell'

app = Pipely::Shell.new(ARGV, Kernel)
prompt = "pipely #{app.id} > "

loop do
  app.interpret(Readline.readline(prompt))
end

